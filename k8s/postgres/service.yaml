apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: lab-devops
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
